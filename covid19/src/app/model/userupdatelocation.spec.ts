import { Userupdatelocation } from './userupdatelocation';

describe('Userupdatelocation', () => {
  it('should create an instance', () => {
    expect(new Userupdatelocation()).toBeTruthy();
  });
});
